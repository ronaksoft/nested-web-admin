import C_PLACE_POLICY  from './../../consts/CPlacePolicy';

interface IPlacePolicy {
    add_post: C_PLACE_POLICY;
    add_place: C_PLACE_POLICY;
    add_member: C_PLACE_POLICY;
}

export default IPlacePolicy;
